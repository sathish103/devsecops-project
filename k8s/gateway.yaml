apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: devsecops-gateway
  namespace: devsecops
spec:
  gatewayclass: aws-alb

  listeners:
    - name: http
      protocal: HTTPS
      port: 80
      allowedRoutes:
        namespace:
          from: same
