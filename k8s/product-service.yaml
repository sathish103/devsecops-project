apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: product-service
  namespace: devsecops

spec:
  replicas: 1

  revisionHistoryLimit: 3

  selector:
    matchLabels:
      app: product-service

  template:
    metadata:
      labels:
        app: product-service

    spec:
      containers:
        - name: product-service
          image: 425034746703.dkr.ecr.ap-south-1.amazonaws.com/devsecops-product-service

          command:
            - java
          args:
            - "-javaagent:/otel/opentelemetry-javaagent.jar"
            - "-jar"
            - "/app/app.jar"

          ports:
            - containerPort: 8082

          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 12

          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3

          env:
            - name: OTEL_SERVICE_NAME
              value: product-service

            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://tempo.tempo.svc.cluster.local:4318

            - name: OTEL_EXPORTER_OTLP_PROTOCOL
              value: http/protobuf

            - name: OTEL_TRACES_EXPORTER
              value: otlp

            - name: OTEL_METRICS_EXPORTER
              value: none

            - name: OTEL_LOGS_EXPORTER
              value: none

            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: devsecops-secrets
                  key: DB_HOST

            - name: DB_NAME
              value: productdb

            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: devsecops-secrets
                  key: DB_PORT

            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: devsecops-secrets
                  key: DB_USERNAME

            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: devsecops-secrets
                  key: DB_PASSWORD

            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: devsecops-secrets
                  key: JWT_SECRET

  strategy:
    blueGreen:
      activeService: product-service
      previewService: product-service-preview

      autoPromotionEnabled: false

      scaleDownDelaySeconds: 30

      previewReplicaCount: 1

---

apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: devsecops
  labels:
    app: product-service

spec:
  selector:
    app: product-service

  ports:
    - name: http
      port: 8082
      targetPort: 8082

  type: ClusterIP

---

apiVersion: v1
kind: Service
metadata:
  name: product-service-preview
  namespace: devsecops
  labels:
    app: product-service

spec:
  selector:
    app: product-service

  ports:
    - name: http
      port: 8082
      targetPort: 8082

  type: ClusterIP